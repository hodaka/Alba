# 2026-07-24 会話ログ（ユーザー発言・無加工）

## 03:47:35

Alba リポジトリ 会話ログ蓄積セットアップ

【前提】
このリポジトリはコードではなく、TRPG/創作の設定資料集。
目的は「私の発言を無加工で蓄積すること」。要約・整形は不要。

【やってほしいこと】

1. ~/.claude/projects/ の存在を確認する。
   ls -la ~/.claude/projects/
   存在しない場合は、それを報告して以降の作業を中止すること。
   代替案の提案は不要。

2. 存在する場合、このプロジェクトに対応するディレクトリを特定し、
   JSONL の構造を確認する。1行だけ出力して見せること。
   全件を読み込まないこと。

3. JSONL から user ロールの発言のみを抽出し、
   logs/YYYY-MM-DD.md に追記するスクリプトを書く。
   - 言語は Python か jq のどちらでも良い
   - 抽出したテキストは一切加工しない
     （整形・要約・句読点調整・誤字修正、すべて禁止）
   - スクリプトは scripts/ に置く
   - あなたが発言内容を出力に含めてはならない。
     必ずプログラムが読み書きすること。

4. Hooks で自動実行できるか調べ、可能なら設定する。
   Stop イベントで発火させたい。

5. 動作確認をして、logs/ に正しく落ちたか報告する。

【禁止事項】
- 指示のないファイルを読まない
- リポジトリ全体の探索・予備調査をしない
- canon-notes.md は存在しない前提で扱う（削除予定）
- 発言内容をあなたが再生成しない

新しいレポジトリを作って始めて
